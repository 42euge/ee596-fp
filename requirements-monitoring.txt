# Monitoring System Requirements
# Install with: pip install -r requirements-monitoring.txt

# Core dependencies (required)
numpy>=1.21.0
scipy>=1.7.0

# W&B integration (optional but recommended)
wandb>=0.15.0

# TensorBoard integration (optional but recommended)
tensorboard>=2.13.0
torch>=2.0.0  # Required for TensorBoard

# Web dashboard (optional but recommended)
streamlit>=1.25.0
plotly>=5.14.0
pandas>=1.5.0

# Additional utilities
python-dateutil>=2.8.0
